@../../CLAUDE.md

## Commit after every change

After completing any task, always commit with a clear message describing what changed and why. Push immediately after.

```bash
git add -A
git commit -m "feat|fix|chore: short description"
git push
```

Never leave work uncommitted at the end of a task.
